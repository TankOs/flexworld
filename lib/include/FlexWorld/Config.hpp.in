#pragma once

#include <FlexWorld/Version.hpp>

namespace flex {

static const Version VERSION( 0, 0, 0 );
static const char* VERSION_SUFFIX = "@FW_VERSION_SUFFIX@";

static const char* ROOT_DATA_DIRECTORY = "@FW_ROOT_DATA_DIRECTORY@";

}
